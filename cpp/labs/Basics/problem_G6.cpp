/*

1) Get Distance in miles
2) Convert miles distance into km distance
3) display distance in km

*/

#include <iostream>

int main()
{

    std::cout << sizeof(double) << std::endl;

    /*
    char letter = 'a';
    std::string app_name = "Distance #88 KM";
    std::string app_name_continue = "Calculator";
    std::string name = app_name + " " + app_name_continue + " ";

    std::uint16_t distance_miles = 0;
    constexpr float MILE_TO_KM = 1.6; //compile time
    std::cout << "Runninng App " << name <<" Please Enter distance in miles" << std::endl;
    std::cin >> distance_miles;

    float distance_km = distance_miles * MILE_TO_KM;
    std::cout << "distance in miles " <<  distance_km << std::endl;

    */
    return 0;
}